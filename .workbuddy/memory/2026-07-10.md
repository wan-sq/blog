# 2026-07-10 工作记录

## 个人博客网站开发
- 需求：个人博客（主题=生活/随笔），含文章列表、文章详情、标签分类、关于页，支持 Markdown 渲染。
- 技术栈：Vite + React 18 + React Router 6（HashRouter，便于静态部署）+ react-markdown + remark-gfm + rehype-highlight。
- 文章数据层：`src/lib/posts.js` 用 `import.meta.glob('../content/posts/*.md', { query:'?raw', eager:true })` 加载；`src/lib/frontmatter.js` 手写极简 frontmatter 解析（支持 title/date/excerpt/tags）。新增文章 = 丢一个 .md 文件。
- 站名/文案集中在 `src/lib/site.js`（标题「拾光随笔」、作者「林小慢」等，待用户自定义）。
- 构建：`npm run build` 成功（dist/）；`npm run preview -- --port 4173 --host` 本地预览可用 http://localhost:4173/。
- 注意：当前 dist JS chunk 511KB（含 highlight.js 全量），后续可按需 code-split 或换轻量高亮。

## 部署 & 内容替换（同日后续）
- 部署：已用 `workbuddy_cloudstudio_deploy` 一键部署 dist/ 到 CloudStudio，沙箱 ID `771ef767792c41eeb84b3161922051ac`，链接 https://771ef767792c41eeb84b3161922051ac.app.codebuddy.work （重部署复用同一沙箱）。
- GitHub Pages：已在 `.github/workflows/deploy.yml` 写好 GitHub Actions 自动部署（push main 即构建+发布 dist/）；base 已设为 './' 适配项目子路径。
- 内容替换：用户要求换成自己的内容（研究/Zotero 方向）。已删除 5 篇生活示例，改为 5 篇：zotero-zhengli（今日 Zotero 整理 30 篇）、yuedu-liucheng、biaoqian-fenlei、yanjiu-zhouji、shiyanshi-zhiwai。site.js 标题改为「研习小记」，about.md 改为研究笔记向。⚠️ site.js 的 author/title 仍是占位（林小慢），待用户改成真名。
